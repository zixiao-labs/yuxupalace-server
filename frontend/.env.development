# Where the unified login center is hosted in dev.
VITE_LOGIN_URL=http://localhost:5173/login

# Backend API base URL (yuxu-server). Leave empty in prod for same-origin.
VITE_API_BASE_URL=http://localhost:8080
